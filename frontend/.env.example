# Development (uses Vite proxy by default; only set this if NOT using the dev server)
# VITE_API_URL=http://localhost:5000/api

# Production - only needed if frontend and backend are deployed on separate domains
# VITE_API_URL=https://your-backend-service-url.railway.app/api
